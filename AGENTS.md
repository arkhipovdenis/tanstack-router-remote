# Project guidance

This repository is a focused library, not an application template. Do not apply
the `ts-clean-template` skill or introduce its application/infrastructure/view
layering here.

Keep the package compact and organised around its public API and integration
boundaries. Preserve the public/internal split documented in
`docs/architecture.md`.
