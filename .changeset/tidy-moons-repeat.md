---
'@tanstack-router-remote/route-tree-adapter': patch
---

Build with rslib instead of `tsc`, and publish the package for the first time.
The output layout, exports map and ESM-only format are unchanged.
